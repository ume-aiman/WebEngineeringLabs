const numbers = [1, 2, 3, 4, 5];

const temp = numbers.map(function(element){
  return element*2;
});
console.log(temp)
// map is high order that pick ananomys function that printssss an array 
// in case if you want new resultant array from another array 